describe('testing artimatika', () => {
  it('should return 4 when adding 2 and 2', () => {
    expect(2 + 2).toBe(4);
  });

  it('should return 5 when adding 2 and 3', () => {
    expect(2 + 3).toBe(5);
  });

  it('should return 6 when adding 3 and 3', () => {
    expect(3 + 3).toBe(6);
  });
});
